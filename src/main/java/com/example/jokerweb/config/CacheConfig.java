package com.example.jokerweb.config;

/**
 * CacheConfig는 RedisConfig로 대체되었습니다.
 * Redis가 설정되어 있으면 Redis를 사용하고, 없으면 Caffeine을 사용합니다.
 * @deprecated 이 클래스는 RedisConfig로 대체되었습니다.
 */
@Deprecated
public class CacheConfig {
    // 이 클래스는 더 이상 사용되지 않습니다.
    // RedisConfig.java를 참조하세요.
}
